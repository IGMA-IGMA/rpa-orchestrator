# RPA Orchestrator
Веб-платформа для управления очередью RPA-ботов (генерация PDF и парсинг email).

## Запуск
1. Скопируйте `.env.example` в `.env` и заполните данные.
2. Соберите контейнеры:
   ```bash
   docker-compose up --build